import React from 'react';

export interface IAppForm {
    header?: string;
    description?: string;
    submitButton?: string;
}

const AppForm: React.FC = () => {
    return <div></div>;
};

export default AppForm;
