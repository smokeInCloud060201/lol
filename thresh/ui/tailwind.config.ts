import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    "./src/**/*.tsx",
    "./**/*.html",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;